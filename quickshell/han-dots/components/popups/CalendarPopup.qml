import "./calendarPopup" as CalendarModule
import QtQuick

// 📅 FORWARDING CALENDAR POPUP WRAPPER (DELEGATES TO COMPONENTS/POPUPS/CALENDARPOPUP/CALENDARPOPUP.QML)
CalendarModule.CalendarPopup {
    id: legacyCalendarPopup
}
