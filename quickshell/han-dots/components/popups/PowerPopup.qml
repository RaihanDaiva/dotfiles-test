import "./powerPopup" as PowerModule
import QtQuick

// 🔌 FORWARDING POWER POPUP WRAPPER (DELEGATES TO COMPONENTS/POPUPS/POWERPOPUP/POWERPOPUP.QML)
PowerModule.PowerPopup {
    id: legacyPowerPopup
}
