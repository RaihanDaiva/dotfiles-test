import "./notificationPopup" as NotifModule
import QtQuick

// 🔔 FORWARDING NOTIFICATION POPUP WRAPPER (DELEGATES TO COMPONENTS/POPUPS/NOTIFICATIONPOPUP/NOTIFICATIONPOPUP.QML)
NotifModule.NotificationPopup {
    id: legacyNotificationPopup
}
